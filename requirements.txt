fastapi==0.110.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.29
psycopg2-binary==2.9.9
python-dotenv==1.0.1
passlib[bcrypt]==1.7.4
# Pin bcrypt to <4.0.0 to remain compatible with Passlib's bcrypt backend
# The bcrypt 4.x releases removed the ``__about__`` module attribute which
# Passlib 1.7 still relies on during backend detection, causing application
# startup to crash.  See https://github.com/pyca/bcrypt/issues/684 for context.
bcrypt==3.2.2
python-jose[cryptography]==3.3.0
httpx==0.27.0
python-multipart==0.0.9
pandas==2.2.2
openpyxl==3.1.2
